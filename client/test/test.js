'use strict';

var expect = require('chai').expect;

describe('raptor-modules/client' , function() {

    it('should implement $rmod based on specification', function(done) {
        expect(true).to.equal(true);
        done();
    });
});